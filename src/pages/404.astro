---
import Layout from '@/layouts/Layout.astro';
import { getTranslations, t } from '@/lib/i18n-server';

const locale = 'en';
const tx = await getTranslations(locale);
---
<Layout title={t(tx, 'notFound.title')} description={t(tx, 'notFound.description')} pageKey="404">
  <section class="flex flex-col items-center justify-center py-32 text-center">
    <h1 class="text-6xl font-bold">404</h1>
    <p class="mt-4 text-xl text-fg-muted">{t(tx, 'notFound.heading')}</p>
    <p class="mt-2 text-fg-muted">{t(tx, 'notFound.subtext')}</p>
    <a href="/" class="btn-primary mt-8">{t(tx, 'notFound.backHome')}</a>
  </section>
</Layout>
