---
// src/components/sections/TrustBar.astro
const services = [
  'Google Analytics', 'Plausible', 'Mixpanel',
  'Stripe', 'PayPal', 'Mailchimp',
  'ConvertKit', 'SendGrid', 'Cloudflare',
  'AWS', 'Google OAuth', 'Facebook Pixel',
];
---
<section class="border-t border-border">
  <div class="mx-auto max-w-6xl px-6 py-14">
    <p class="text-center text-xs uppercase tracking-widest text-fg-faint mb-6" data-i18n="trustBar.label">Works with the tools you already use</p>
    <div class="flex flex-wrap items-center justify-center gap-x-6 gap-y-3">
      {services.map((s) => (
        <span class="text-sm text-fg-muted/80 font-mono">{s}</span>
      ))}
      <span class="text-sm text-fg-faint font-mono italic" data-i18n="trustBar.more">+ many more</span>
    </div>
  </div>
</section>
